# 脱敏层（CRG sidecar）环境变量示例
#
# 用法：把这些键**追加**到服务器现有的 /srv/apps/axonhub/.env 中（不要新建一份 .env，
# 现有文件里还有 DB_PASSWORD、AXONHUB_IMAGE 等键）。真实主机名只写在服务器上，
# 本文件永远只放占位值。
#
# 本文件也用于本地 `docker compose config` 渲染检查，见 ../README.md。

# ── 必填 ───────────────────────────────────────────────────────────────────────

# 允许转发到的上游主机，逗号分隔，**不带协议、不带路径、不带端口**。
#
# 关键语义（已核对 crg/worker.js 的 allowedHost）：
#   1. 留空或不设 => CRG 允许一切上游，即开放代理。overlay 用 ${REDACT_ALLOWED_HOSTS:?}
#      强制非空，就是为了让这种情况在 `docker compose config` 阶段直接失败。
#   2. 匹配规则是「精确主机名 或 其子域」。写 example.com 会连带放行
#      a.example.com、b.example.com。因此**只写精确主机名**，不要写可能过宽的父域。
#
# 取值来源：所有「受保护渠道」的上游主机集合（含已禁用但未归档的渠道），
# 加上验证阶段的回显主机 echo。用 scripts/check-trust-boundary.sh 核对是否漂移。
# 形状示例（真实列表只写服务器 .env，不要把真实主机名提交进仓库）：
#   REDACT_ALLOWED_HOSTS$host_a,$host_b,echo
# 本文件给 compose --env-file 用，值必须非空；env-file 不做 shell 展开，
# 因此下面这一行的字面值本身就是占位，不是真实主机列表。
REDACT_ALLOWED_HOSTS=${REDACT_SMOKE_HOSTS:-echo}

# ── 可选（留空即用 CRG 默认值）────────────────────────────────────────────────

# 单请求正文上限（字节）。默认 16777216 = 16 MiB。超出返回 413 且不转发。
# 需要容纳 >=1 MiB 的大文件工具结果往返，不要调小。
REDACT_MAX_BODY_BYTES=16777216

# 单请求唯一敏感值数量上限。默认 16384。超出返回 413 且不转发。
REDACT_MAX_REDACTIONS=16384

# 响应的 access-control-allow-origin 取值，默认 *。
# 脱敏层只在内部网络被 axonhub 调用，不面向浏览器，通常无需修改。
# REDACT_CORS_ORIGIN=*

# ── 说明 ──────────────────────────────────────────────────────────────────────
#
# HOST / PORT 不在此处配置：overlay 已固定 HOST=0.0.0.0、PORT=8787。
# HOST 必须是 0.0.0.0，否则 CRG 只监听回环地址，同网络的 axonhub 容器连不上。
#
# 注入给模型的英文提示文案（REDACT_NOTICE）是 worker.js 里的常量，不是环境变量，
# 无法在此调整。详见 crg/UPSTREAM.md 的「已知限制」。
