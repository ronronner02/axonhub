# 本目录产物要在 Linux 服务器上直接执行，任何 CRLF 都会让 bash/sh 报错、让 Dockerfile 解析失败。
# 本机 core.autocrlf=true 会在 checkout 时把 LF 换成 CRLF，这里强制回 LF。
* text=auto eol=lf
*.sh text eol=lf
*.mjs text eol=lf
*.js text eol=lf
*.yml text eol=lf
*.json text eol=lf
*.md text eol=lf
Dockerfile text eol=lf
.env.example text eol=lf
SHA256SUMS text eol=lf
LICENSE text eol=lf
# 根 .gitignore 用 "*" + "!*.*" 只放行带扩展名的文件；SHA256SUMS 无扩展名，
# 已用 git add -f 显式跟踪。若新增其它无扩展名文件，同样需要 -f。
